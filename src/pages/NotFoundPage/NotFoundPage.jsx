import React from 'react';
import Heading from '../../components/Heading/Heading';
import { Section } from './NotFoundPage.styled';

const NotFoundPage = () => {
  return (
    <Section>
      <Heading>Не найдена страница</Heading>
    </Section>
  );
};

export default NotFoundPage;
